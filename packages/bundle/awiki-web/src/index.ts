/**
 * @deepseek-ai/dsh-awiki-web — an optional AWiki patch layer for a Web
 * profile. The package's runtime content is `cordis.patch.yml`, declared by
 * the `dsh.bundle.patch` manifest field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-awiki-web
 */

export {}
