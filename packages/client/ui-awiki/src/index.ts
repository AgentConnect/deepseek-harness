/**
 * AWiki browser surface, node half. The browser bundle is discovered through
 * the package's `dsh.client` declaration; all AWiki credentials and network
 * operations remain in the Host plugin.
 */

/** Host plugin body; this UI package has no Host behavior. */
export function apply(): void {}
